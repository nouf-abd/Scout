import 'package:flutter/material.dart';

class CoachPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Coach Page')),
      body: Center(child: Text('Work in Progress', style: TextStyle(fontSize: 24))),
    );
  }
}
